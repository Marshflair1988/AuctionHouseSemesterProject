{
  "name": "auction-house",
  "version": "1.0.0",
  "description": "Auction House Project",
  "scripts": {
    "build": "node build.js",
    "dev": "npx tailwindcss -i ./css/input.css -o ./css/style.css --watch & live-server",
    "watch": "npx tailwindcss -i ./css/input.css -o ./css/style.css --watch"
  },
  "dependencies": {
    "fs": "0.0.1-security",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.1"
  }
}
